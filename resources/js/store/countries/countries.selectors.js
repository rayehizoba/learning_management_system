export const selectCountriesFetch = (state) => state.countries.fetch;
export const selectCountriesFetchSuccess = (state) => state.countries.fetchSuccess;
export const selectCountries = (state) => state.countries.collection;
