export const selectQuizzes = (state) => state.quizzes.collection;

export const selectQuizzesFetch = (state) => state.quizzes.fetch;

export const selectQuizzesFetchSuccess = (state) => state.quizzes.fetchSuccess;

export const selectQuizzesFetchError = (state) => state.quizzes.fetchError;

