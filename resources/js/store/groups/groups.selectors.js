export const selectGroups = (state) => state.groups.collection;
export const selectGroupsFetch = (state) => state.groups.fetch;
export const selectGroupsFetchSuccess = (state) => state.groups.fetchSuccess;
export const selectGroupsFetchError = (state) => state.groups.fetchError;
export const selectGroupsPagination = (state) => state.groups.pagination;
