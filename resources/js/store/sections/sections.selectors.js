export const selectSections = (state) => state.sections.collection;

export const selectSectionsFetch = (state) => state.sections.fetch;

export const selectSectionsFetchSuccess = (state) => state.sections.fetchSuccess;

export const selectSectionsFetchError = (state) => state.sections.fetchError;

