export const selectCourses = (state) => state.courses.collection;

export const selectCoursesFetch = (state) => state.courses.fetch;

export const selectCoursesFetchSuccess = (state) => state.courses.fetchSuccess;

export const selectCoursesFetchError = (state) => state.courses.fetchError;

